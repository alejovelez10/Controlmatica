<%= javascript_pack_tag 'Reports' %>
<%= react_component('Reports', usuario: current_user, users: User.select(:id, :names).map { |u| { id: u.id, names: u.names } }, clientes: Customer.select(:id, :name).map { |c| { id: c.id, name: c.name } }, estados: @estados, rol: current_user.rol) %>
